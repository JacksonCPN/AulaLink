import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.aulalink',
  appName: 'aulalink',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
